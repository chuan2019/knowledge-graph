global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090

  - job_name: kg-qa-api
    metrics_path: /api/v1/metrics/prometheus
    static_configs:
      - targets:
          - api:8000
